<!DOCTYPE html>
<html lang="en" dir="ltr">
 <head>
   <meta charset="utf-8">
   <title>Jessamyn's Fridge</title>
 </head>
 <body>
<h1>Items</h1>
<ul>
      <% for(let i = 0; i< items.length ; i ++) { %>
      <li> <a href="/fridge/<%=i%>"><%=items[i].name%></a></li>
      <li> <a href="/fridge/<%=i%>"><%=items[i].type%></a></li>
      <li><% if(items[i].edible === true){ %>
        It is edible
      <% } else { %>
        It is not edible
      <% } %></li>
       <img src='<%=items[i].img%>' style= "height: 200px"> 
     <% } %>
  </ul>
 </body>
</html>